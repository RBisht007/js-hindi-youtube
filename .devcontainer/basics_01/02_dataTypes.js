"use strict"; // treat all JS code as newer version
//alert("hello worlld")// we are using nodee js not the browwser so alert wont work

let name="rakhi"// string literal
let age=22 // number literal
let isApproved=true// boolean literal
let firstName=undefined // undefined literal
let selectedColor=null // null literal

// we can use typeof operator to know the data type of varuiable
console.log(typeof age)
console.log(typeof name)






